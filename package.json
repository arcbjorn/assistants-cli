{
  "name": "assistants-cli",
  "module": "cli.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build:bin": "bun build --compile cli.ts --outfile dist/assistants-cli",
    "update:docs": "bun run update-agent.ts",
    "test": "bun test",
    "build:cli": "bun build --compile cli.ts --outfile dist/assistants-cli"
  }
}
